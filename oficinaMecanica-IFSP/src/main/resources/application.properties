# ================================================
# PROFILE: dev — MySQL (mecanica_IFSP)
# ================================================
# Para ativar: altere application.properties para
#   spring.profiles.active=dev

# DATASOURCE
spring.datasource.url=jdbc:mysql://localhost:3306/mecanica_IFSP?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Mmd76448401#

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# MAIL — Gmail SMTP
# Configure as variáveis de ambiente MAIL_USERNAME e MAIL_PASSWORD
# No Gmail: Conta > Segurança > Senhas de app (requer 2FA ativado)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:matheusmdomingues.cursos@gmail.com}
spring.mail.password=${MAIL_PASSWORD:bewzoclzdwutnwwa}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8

# ADMIN padrão (usado pelo DataInitializer na primeira execução)
app.admin.email=${ADMIN_EMAIL:admin@oficina.com}

# JWT
# Obrigatório em produção: defina a variável de ambiente JWT_SECRET
# Gerar novo segredo: openssl rand -base64 32
jwt.secret=oficina-mecanica-ifsp-chave-secreta-2026
jwt.expiration=${JWT_EXPIRATION:86400000}
